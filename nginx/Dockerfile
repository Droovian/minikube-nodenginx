FROM nginx:latest
COPY default.conf /etc/nginx/conf.d/default.conf
# This copies your default.conf file from your project folder (where the Dockerfile is) into the container’s NGINX config directory.